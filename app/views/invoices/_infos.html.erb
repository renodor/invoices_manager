<div class="invoice-infos">
  <p>N° de Facture : <%= @invoice.number %></p>
  <p>Date d'émission : <%= @invoice.date.strftime("%d/%m/%Y") %></p>
  <div class="invoice-infos__name"><%= @invoice.title %></div>
  <%= link_to '',
    [:edit_infos, invoice],
    class: 'full-absolute' %>
</div>