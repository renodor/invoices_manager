<main class="container">
  <div class="header">
    <h1>Invoices</h1>
    <%= link_to 'New invoice',
                new_invoice_path,
                class: 'btn btn--primary' %>
  </div>

  <%= render partial: 'invoice_card', collection: @invoices, as: :invoice %>
</main>